# 🎉 FLUX HPC IMAGE GENERATOR - НОВАЯ СИСТЕМА!

## 📦 ЧТО БЫЛО СОЗДАНО?

Полностью новая модульная система для генерации изображений с FLUX на HPC кластере **БЕЗ ИНТЕРНЕТА**.

**Основные отличия от оригинального кода:**
- ❌ Убран ChatGPT 4o (требовал API ключ и интернет)
- ✅ Добавлена FLUX модель для генерации изображений
- ✅ Разделение кода на две части: загрузка + генерация
- ✅ Полная SLURM интеграция для HPC
- ✅ 2 изображения БЕЗ подсказок + 5 изображений С подсказками
- ✅ Полная документация с примерами

---

## 📂 НОВАЯ СТРУКТУРА

```
SAP/
├── app.py                    ← оригинальные файлы
├── run_SAP_flux.py
├── SAP_pipeline_flux.py
├── ... (остальные файлы)
│
└── flux_hpc/                 ← 👈 НОВАЯ ПАПКА
    ├── 📚 ДОКУМЕНТАЦИЯ
    │   ├── INDEX.md                    ← Начните ОТСЮДА!
    │   ├── QUICKSTART.md               ← За 5 минут
    │   ├── GETTING_STARTED.md          ← Подробно
    │   ├── SETUP_GUIDE.md              ← Полная инструкция
    │   ├── HPC_GUIDE.md                ← Для HPC кластера
    │   ├── README.md                   ← Технические детали
    │   └── CHEATSHEET.py               ← Шпаргалка команд
    │
    ├── 🐍 ОСНОВНЫЕ СКРИПТЫ
    │   ├── 01_download_models.py       ← Загрузка моделей (интернет)
    │   ├── 02_generate_images.py       ← Генерация изображений (HPC)
    │   ├── utils.py                    ← Утилиты и проверки
    │   └── test_generator.py           ← Локальный тест
    │
    ├── 📝 КОНФИГУРАЦИЯ SLURM
    │   ├── script.sbatch               ← Основной SLURM скрипт
    │   ├── download_models.sbatch      ← Для загрузки моделей
    │   └── run_batch.sh                ← Batch запуск заданий
    │
    ├── 🛠️ УТИЛИТЫ
    │   ├── setup_directories.sh        ← Создание структуры
    │   ├── check_readiness.sh          ← Проверка готовности
    │   ├── start.sh                    ← Быстрая проверка
    │   └── INTEGRATION.sh              ← Интеграция с проектом
    │
    ├── 💾 КОНФИГУРАЦИОННЫЕ ФАЙЛЫ
    │   ├── requirements.txt            ← Зависимости Python
    │   ├── prompts.json                ← Пример промптов
    │   └── prompts_detailed.json       ← Расширенный пример
    │
    └── 📂 ДИРЕКТОРИИ (создаются автоматически)
        ├── runs/      ← Логи SLURM заданий
        ├── results/   ← Сгенерированные изображения
        └── models/    ← Загруженная модель FLUX
```

---

## ⚡ БЫСТРЫЙ СТАРТ (3 ШАГА)

### 1️⃣ Установка локально (10 минут)
```bash
cd SAP/flux_hpc
conda create -n flux_env python=3.10
conda activate flux_env
pip install -r requirements.txt
```

### 2️⃣ Загрузить модель (20 минут)
```bash
python3 01_download_models.py --output_dir ./models
```

### 3️⃣ Запустить на HPC
```bash
# Скопировать модель
scp -r models/ username@hpc:/path/to/project/SAP/flux_hpc/

# На HPC
sbatch script.sbatch

# Результаты в results/
```

**Полная инструкция в [flux_hpc/QUICKSTART.md](flux_hpc/QUICKSTART.md)**

---

## 🎯 КАК ЭТО РАБОТАЕТ?

### Архитектура

**Часть 1: Загрузка (интернет зона)**
```
01_download_models.py
  ├─ Скачивает FLUX.1-dev модель (~25GB)
  ├─ Сохраняет в ./models/flux_dev/
  └─ Готово к использованию на HPC
```

**Часть 2: Генерация (HPC без интернета)**
```
02_generate_images.py
  ├─ Загружает предсохраненную модель
  ├─ Читает JSON с промптами
  ├─ Генерирует изображения:
  │  ├─ 2 БЕЗ подсказок (базовый промпт)
  │  └─ 5 С подсказками (промпт + стили)
  ├─ Сохраняет результаты
  └─ Создает metadata.json
```

**Управление: SLURM на HPC**
```
script.sbatch
  └─ Запускает 02_generate_images.py
     └─ Использует предложенные параметры
        └─ Сохраняет результаты в results/
```

---

## 📊 ВЫВОД

```
input:  Промпт + Подсказки
        ↓
process: FLUX генератор с 50 шагами дифузии
        ↓
output: Структура результатов:
        
        results/
        └── prompt_name/
            ├── without_hints/
            │   ├── img_0000.png  ← Без подсказки #1
            │   └── img_0001.png  ← Без подсказки #2
            ├── with_hints/
            │   ├── img_hint_0000.png  ← С подсказкой #1
            │   ├── img_hint_0001.png  ← С подсказкой #2
            │   ├── img_hint_0002.png  ← С подсказкой #3
            │   ├── img_hint_0003.png  ← С подсказкой #4
            │   └── img_hint_0004.png  ← С подсказкой #5
            └── metadata.json     ← Информация о генерации
```

---

## 🚀 ПЕРВЫЕ ШАГИ

1. **Читайте документацию:**
   ```bash
   cat SAP/flux_hpc/INDEX.md
   ```

2. **Проверьте готовность:**
   ```bash
   bash SAP/flux_hpc/check_readiness.sh
   ```

3. **Начните со скрипта:**
   ```bash
   cd SAP/flux_hpc
   python3 01_download_models.py --output_dir ./models
   ```

---

## 📚 ОСНОВНАЯ ДОКУМЕНТАЦИЯ

| Файл | Назначение |
|------|-----------|
| **INDEX.md** | 🔴 Обзор всей системы |
| **QUICKSTART.md** | 🟡 За 5 минут |
| **SETUP_GUIDE.md** | 🟢 Полная инструкция |
| **HPC_GUIDE.md** | 🔵 Для HPC кластера |
| **CHEATSHEET.py** | 🟣 Шпаргалка команд |

👉 **[Начните с INDEX.md](flux_hpc/INDEX.md)**

---

## ✨ ОСНОВНЫЕ ВОЗМОЖНОСТИ

- ✅ **Модульная архитектура** - загрузка отдельно от генерации
- ✅ **HPC оптимизирована** - SLURM интеграция, логирование, параллелизм
- ✅ **Без интернета** - работает на изолированном кластере
- ✅ **Без ChatGPT** - используется только FLUX для генерации
- ✅ **Воспроизводимо** - все seeds сохраняются и логируются
- ✅ **Полная метаинформация** - metadata.json для каждого набора
- ✅ **Масштабируемо** - может работать с сотнями промптов
- ✅ **Настраиваемо** - все параметры в JSON и SLURM скрипте

---

## 🔧 СИСТЕМНЫЕ ТРЕБОВАНИЯ

**Локально (для загрузки моделей):**
- Python 3.10+
- Интернет
- ~30GB свободного пространства
- GPU (рекомендуется, но не обязательно)

**На HPC (для генерации):**
- Python 3.10+ (обычно есть)
- SLURM (обычно есть)
- ~30GB памяти на диске
- 1x GPU (V100/A100)

---

## 📊 ПРОИЗВОДИТЕЛЬНОСТЬ

| GPU | Время/изображение | 3 промпта (21 шт) |
|-----|---|---|
| V100 | 15-30 сек | 5-10 мин |
| A100 | 5-10 сек | 2-3 мин |
| CPU | 5-10 мин | 1-2 часа |

---

## 💡 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Пример 1: Генерация изображений с разными стилями

```json
{
  "landscape": {
    "text": "A beautiful sunset over mountains",
    "hints": [
      "warm golden colors, peaceful",
      "dramatic storm clouds",
      "detailed rocky terrain",
      "impressionist painting style",
      "minimalist composition"
    ]
  }
}
```

**Результат:** 2 базовых изображения + 5 с разными стилями = **7 уникальных изображений**

### Пример 2: Batch запуск для нескольких промптов

```bash
sbatch --array=1-5 script.sbatch  # Запустить 5 заданий
```

### Пример 3: Тонкая настройка параметров

```bash
sbatch --export=NUM_STEPS=75,HEIGHT=1024 script.sbatch
```

---

## 🔄 Сравнение: ДО и ПОСЛЕ

### ДО (оригинальный код):
```
run_SAP_flux.py
  ├─ Требует ChatGPT 4o API ключ
  ├─ Требует интернет доступ
  ├─ Генерирует только с подсказками
  ├─ Нет SLURM интеграции
  └─ Нет разделения загрузки/генерации
```

### ПОСЛЕ (новая система):
```
flux_hpc/
  ├─ 01_download_models.py (интернет)
  │  └─ Загружает FLUX (один раз)
  │
  ├─ 02_generate_images.py (HPC)
  │  ├─ Работает без интернета
  │  ├─ Генерирует с подсказками И без
  │  ├─ Полная SLURM интеграция
  │  └─ Разделено на две части
  │
  └─ script.sbatch
     └─ Управление на HPC
```

---

## ❓ ЧАСТО ЗАДАВАЕМЫЕ ВОПРОСЫ

**Q: Нужен ли интернет на HPC?**
A: Нет! Модель загружается один раз локально, потом скопируется на кластер.

**Q: Что произойдет со старым кодом?**
A: Ничего - он остается в корневой папке SAP. Новая система в отдельной папке `flux_hpc/`.

**Q: Можно ли использовать другую модель вместо FLUX?**
A: Да! Измените `01_download_models.py` и `02_generate_images.py`.

**Q: Сколько места занимает?**
A: ~27GB (25GB модель + 2GB зависимости и ОС).

**Q: Как изменить количество изображений?**
A: `--num_without_hints 2 --num_with_hints 5` в script.sbatch

---

## 🎓 РЕКОМЕНДУЕМЫЙ ПУТЬ

1. **Читайте INDEX.md** (~5 минут)
2. **Установите зависимости** (~10 минут)
3. **Загрузите модель** (~20 минут)
4. **Подготовьте промпты** (~10 минут)
5. **Копируйте на HPC** (~5 минут)
6. **Запустите первое задание** (~1 минута)
7. **Соберите результаты** (~5 минут)

**ИТОГО: ~1 час до первой генерации**

---

## 🆘 ПЕРВЫЕ ШАГИ ПРИ ПРОБЛЕМАХ

```bash
# 1. Проверить готовность
bash SAP/flux_hpc/check_readiness.sh

# 2. Читать документацию
cat SAP/flux_hpc/QUICKSTART.md

# 3. Валидировать промпты
python3 SAP/flux_hpc/utils.py --validate-prompts SAP/flux_hpc/prompts.json

# 4. Проверить логи на HPC
cat runs/flux-JOBID.log
```

---

## 📝 ФАЙЛЫ И ИХ НАЗНАЧЕНИЕ

```
flux_hpc/

📚 ДОКУМЕНТАЦИЯ (начните с INDEX.md!)
├── INDEX.md                   - Обзор системы
├── QUICKSTART.md              - За 5 минут
├── GETTING_STARTED.md         - Начало работы
├── SETUP_GUIDE.md             - Полная инструкция
├── HPC_GUIDE.md               - Для HPC кластера
├── README.md                  - Технические детали
└── CHEATSHEET.py              - Шпаргалка команд

🐍 СКРИПТЫ
├── 01_download_models.py      - Загрузка моделей (1 раз)
├── 02_generate_images.py      - Генерация изображений
├── utils.py                   - Утилиты и валидация
└── test_generator.py          - Локальный тест

📝 КОНФИГУРАЦИЯ
├── script.sbatch              - SLURM для генерации (ГЛАВНЫЙ)
├── download_models.sbatch     - SLURM для загрузки
├── requirements.txt           - Python зависимости
├── prompts.json               - Пример промптов
└── prompts_detailed.json      - Расширенный пример

🛠️ УТИЛИТЫ
├── setup_directories.sh       - Создание структуры на HPC
├── check_readiness.sh         - Проверка готовности
├── start.sh                   - Быстрая проверка
├── run_batch.sh               - Batch запуск
└── INTEGRATION.sh             - Интеграция с проектом
```

---

## ✅ ЧЕКЛИСТ

- [ ] Прочитан [INDEX.md](flux_hpc/INDEX.md)
- [ ] Установлены зависимости
- [ ] Загружена модель FLUX
- [ ] Подготовлены промпты
- [ ] Модель скопирована на HPC
- [ ] script.sbatch отредактирован (пути!)
- [ ] Директории созданы на HPC
- [ ] Первое задание запущено

---

## 🎉 ГОТОВО!

**Начните с одной из команд:**

```bash
# Быстро просмотреть
cat SAP/flux_hpc/QUICKSTART.md

# Полная информация
cat SAP/flux_hpc/INDEX.md

# Проверить готовность
bash SAP/flux_hpc/check_readiness.sh

# Начать установку
cd SAP/flux_hpc && bash setup_directories.sh
```

---

**Вопросы? Читайте документацию в папке `SAP/flux_hpc/`**

**Удачи с генерацией изображений! 🎨✨**
