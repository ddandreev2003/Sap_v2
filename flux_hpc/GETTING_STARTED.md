# 🎉 FLUX HPC - СИСТЕМА ГОТОВА!

## 📦 Что было создано?

Полная модульная система для генерации изображений на HPC кластере без интернета.

**Структура:**
```
SAP/flux_hpc/  ← НОВАЯ ПАПКА
├── 🚀 БЫСТРЫЙ СТАРТ
│   ├── INDEX.md              ← 👈 НАЧНИТЕ ОТСЮДА!
│   ├── QUICKSTART.md         ← За 5 минут
│   └── start.sh              ← Проверка готовности
│
├── 📖 ДОКУМЕНТАЦИЯ
│   ├── SETUP_GUIDE.md        ← Полная инструкция
│   ├── HPC_GUIDE.md          ← Для HPC кластера
│   └── README.md             ← Технические детали
│
├── 🐍 СКРИПТЫ
│   ├── 01_download_models.py         ← Загрузка моделей (интернет)
│   ├── 02_generate_images.py         ← Генерация (HPC без интернета)
│   ├── utils.py                      ← Утилиты
│   └── test_generator.py             ← Локальный тест
│
├── 📝 КОНФИГУРАЦИЯ
│   ├── script.sbatch                 ← SLURM для генерации
│   ├── download_models.sbatch        ← SLURM для загрузки
│   ├── setup_directories.sh          ← Создание папок
│   ├── run_batch.sh                  ← Batch запуск
│   ├── check_readiness.sh            ← Проверка готовности
│   └── start.sh                      ← Быстрая проверка
│
├── 💾 ДАННЫЕ
│   ├── prompts.json                  ← Пример промптов
│   ├── prompts_detailed.json         ← Расширенный пример
│   ├── requirements.txt              ← Зависимости
│   └── (можно удалить после использования)
│
└── 📂 ДИРЕКТОРИИ (создаются автоматически)
    ├── runs/      ← Логи SLURM заданий
    ├── results/   ← Сгенерированные изображения
    └── models/    ← Загруженная модель FLUX
```

---

## ⚡ БЫСТРЫЙ СТАРТ (5 МИНУТ)

### Шаг 1: Прочитайте INDEX.md

```bash
cat SAP/flux_hpc/INDEX.md
```

### Шаг 2: Установите зависимости

```bash
cd SAP/flux_hpc
conda create -n flux_env python=3.10
conda activate flux_env
pip install -r requirements.txt
```

### Шаг 3: Загрузите модель (ОДИН РАЗ)

```bash
python3 01_download_models.py --output_dir ./models
```

### Шаг 4: Подготовьте промпты

```bash
# Создать шаблон или отредактировать существующий
python3 utils.py --create-template my_prompts.json
python3 utils.py --validate-prompts my_prompts.json
```

### Шаг 5: На HPC кластере

```bash
# Скопировать
scp -r models/ username@hpc:/path/to/project/flux_hpc/

# Подключиться
ssh username@hpc
cd /path/to/project/flux_hpc
bash setup_directories.sh

# Отредактировать пути в script.sbatch
vim script.sbatch

# Запустить
sbatch script.sbatch
```

---

## 📚 ПОЛНАЯ ДОКУМЕНТАЦИЯ

| Документ | Когда читать | О чем |
|----------|---|---|
| **INDEX.md** | 🔴 ПЕРВЫЙ | Обзор и рекомендации |
| **QUICKSTART.md** | 🟡 ВТОРОЙ | За 5 минут до первой генерации |
| **SETUP_GUIDE.md** | 🟢 ТРЕТИЙ | Полная пошаговая инструкция |
| **HPC_GUIDE.md** | 🔵 ЧЕТВЕРТЫЙ | Для работы на кластере |
| **README.md** | 🟣 ПЯТЫЙ | Технические детали |

**Начните с [INDEX.md](INDEX.md)!**

---

## 🎯 ЧТО ДЕЛАЕТ СИСТЕМА?

### Простыми словами:

**ВВОД:** 
```
Промпт: "A sunset over mountains"
Подсказки: ["warm colors", "dramatic", "detailed", ...]
```

**ПРОЦЕСС:**
- Генерирует 2 изображения **БЕЗ подсказок** (базовый промпт)
- Генерирует 5 изображений **С подсказками** (промпт + разные стили)
- Каждое изображение уникально (разные seeds)
- Сохраняет метаданные о всех параметрах

**ВЫВОД:**
```
results/
└── prompt_name/
    ├── without_hints/
    │   ├── img_0000.png
    │   └── img_0001.png
    ├── with_hints/
    │   ├── img_hint_0000.png
    │   ├── img_hint_0001.png
    │   ├── img_hint_0002.png
    │   ├── img_hint_0003.png
    │   └── img_hint_0004.png
    └── metadata.json
```

---

## ✨ ОСОБЕННОСТИ

- ✅ **Модульная архитектура** - разделение загрузки и генерации
- ✅ **HPC оптимизирована** - SLURM интеграция, логирование
- ✅ **Без интернета** - работает на изолированном кластере
- ✅ **Без ChatGPT** - только FLUX для генерации
- ✅ **Воспроизводимо** - фиксированные seeds
- ✅ **Полностью настраиваемо** - все параметры в JSON и SLURM
- ✅ **Масштабируемо** - может работать с множеством промптов

---

## 🚀 ОСНОВНЫЕ КОМАНДЫ

```bash
# ЛОКАЛЬНО (с интернетом)
python3 01_download_models.py --output_dir ./models
python3 utils.py --check-env
python3 utils.py --validate-prompts prompts.json
python3 test_generator.py --model_path ./models/flux_dev

# НА HPC
bash setup_directories.sh
sbatch script.sbatch
squeue -u $USER
tail -f runs/flux-*.log
```

---

## 📊 ОЦЕНКА ВРЕМЕНИ

| Этап | Время |
|------|-------|
| Установка зависимостей | 5-10 минут |
| Загрузка модели FLUX (25GB) | 10-20 минут |
| Подготовка промптов | 5 минут |
| Генерация 3 промптов на V100 | 5-10 минут |
| Генерация 3 промптов на A100 | 2-3 минуты |

**ИТОГО ДО ПЕРВОЙ ГЕНЕРАЦИИ:** ~30 минут

---

## 🔑 КЛЮЧЕВЫЕ ФАЙЛЫ

### Для локального использования:
- `01_download_models.py` - загрузка моделей (запустить один раз)
- `utils.py` - проверка окружения и валидация промптов
- `test_generator.py` - локальное тестирование

### Для HPC:
- `02_generate_images.py` - основной генератор (запускается автоматически)
- `script.sbatch` - SLURM конфигурация для запуска
- `setup_directories.sh` - инициализация директорий

### Конфигурация:
- `prompts.json` - примеры промптов
- `requirements.txt` - зависимости Python

---

## ❓ ЧАСТО ЗАДАВАЕМЫЕ ВОПРОСЫ

**Q: Нужен ли интернет на HPC?**
A: Нет! Модель загружается один раз локально, потом скопируется на кластер.

**Q: Сколько дисковой памяти нужно?**
A: ~27GB (25GB модель + 2GB ОС и зависимости)

**Q: Какой GPU нужен?**
A: V100 (32GB VRAM) рекомендуется. A100 даст ускорение в 3x.

**Q: Можно ли запустить без GPU?**
A: Можно на CPU, но очень медленно (часы вместо минут).

**Q: Как изменить количество изображений?**
A: `--num_without_hints 2 --num_with_hints 5` в script.sbatch

**Q: Где найти результаты?**
A: В папке `results/` - автоматически создается с нужной структурой.

---

## 🆘 ПЕРВЫЕ ШАГИ ПРИ ПРОБЛЕМАХ

```bash
# 1. Проверить готовность
bash check_readiness.sh

# 2. Проверить окружение
python3 utils.py --check-env

# 3. Проверить промпты
python3 utils.py --validate-prompts prompts.json

# 4. Локальный тест
python3 test_generator.py --model_path ./models/flux_dev

# 5. Проверить логи на HPC
cat runs/flux-JOBID.err
```

---

## 📖 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- [FLUX GitHub](https://github.com/black-forest-labs/flux)
- [FLUX Paper](https://arxiv.org/abs/2406.16247)
- [Diffusers Docs](https://huggingface.co/docs/diffusers)
- [SLURM Manual](https://slurm.schedmd.com/)

---

## ✅ ЧЕКЛИСТ ПЕРЕД ПЕРВЫМ ЗАПУСКОМ

- [ ] Прочитаны INDEX.md и QUICKSTART.md
- [ ] Установлены зависимости (`pip install -r requirements.txt`)
- [ ] Загружена модель (`python3 01_download_models.py`)
- [ ] Подготовлены промпты (JSON файл валидирован)
- [ ] Модель скопирована на HPC
- [ ] Отредактирован script.sbatch (пути и параметры)
- [ ] Созданы директории на HPC (`bash setup_directories.sh`)
- [ ] Готово к запуску!

---

## 🎓 РЕКОМЕНДУЕМЫЙ ПУТЬ

### День 1: Установка
1. Прочитайте INDEX.md
2. Установите Python 3.10+ и зависимости
3. Запустите check_readiness.sh

### День 2: Подготовка
1. Загрузите модель FLUX (30 минут)
2. Подготовьте свои промпты
3. Валидируйте их

### День 3: HPC запуск
1. Скопируйте файлы на кластер
2. Отредактируйте script.sbatch
3. Запустите sbatch script.sbatch

### День 4: Результаты
1. Соберите результаты с HPC
2. Проверьте качество
3. При необходимости - повторите с другими параметрами

---

## 🚀 ГОТОВО К ИСПОЛЬЗОВАНИЮ!

**👉 [Начните с INDEX.md](INDEX.md)**

```bash
# Или быстро проверьте готовность:
bash SAP/flux_hpc/check_readiness.sh
```

---

**Вопросы? Читайте документацию в папке `SAP/flux_hpc/`**

**Удачи с генерацией изображений! 🎨✨**
