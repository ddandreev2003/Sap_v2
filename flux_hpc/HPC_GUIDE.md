# HPC Кластер - Полная инструкция

## 📋 Предварительные требования

- ✅ Модель FLUX загружена (из `01_download_models.py`)
- ✅ Промпты подготовлены (JSON файл)
- ✅ Файлы скопированы на HPC

---

## 🔐 Подготовка на HPC

### 1. Подключиться к HPC

```bash
ssh username@hpc.cluster
```

### 2. Перейти в директорию проекта

```bash
cd /path/to/project/flux_hpc
```

### 3. Создать необходимые директории

```bash
bash setup_directories.sh
```

Должны появиться:
```
✅ Директории созданы:
   📂 runs/ - для логов и ошибок
   📂 results/ - для сохранения результатов
   📂 models/ - для сохранения моделей
```

---

## ⚙️ Конфигурация перед запуском

### Отредактировать `script.sbatch`

**ВАЖНО**: Заменить `<пути>` на абсолютные пути вашего проекта!

```bash
vim script.sbatch
```

Найти эту строку и заменить:

```bash
# БЫЛО:
cd /path/to/SAP/flux_hpc

# СТАЛО (пример):
cd /home/username/project/SAP/flux_hpc
```

И эту:

```bash
# БЫЛО:
python3 02_generate_images.py \
    --model_path /path/to/models/flux_dev \
    --prompts_file ./prompts.json \

# СТАЛО:
python3 02_generate_images.py \
    --model_path /home/username/project/SAP/flux_hpc/models/flux_dev \
    --prompts_file /home/username/project/SAP/flux_hpc/prompts.json \
```

**Совет**: Используйте `pwd` чтобы узнать полный путь:
```bash
pwd  # Выведет что-то типа: /home/username/project/SAP/flux_hpc
```

---

## 🚀 Запуск задания

### Вариант 1: Простой запуск

```bash
sbatch script.sbatch
```

### Вариант 2: С переопределением параметров

```bash
sbatch --job-name=my_flux_job \
       --time=3:00:00 \
       --output=runs/custom-%j.log \
       script.sbatch
```

### Вариант 3: Массив заданий (для разных промптов)

```bash
sbatch --array=1-5 script.sbatch
```

---

## 📊 Мониторинг выполнения

### Просмотр всех заданий

```bash
squeue -u $USER
```

**Ожидаемый вывод:**
```
             JOBID PARTITION     NAME     USER ST       TIME  NODES NODELIST(REASON)
          12345678      gpu flux_image  username  R       2:34      1 node-001
```

### Подробная информация о задании

```bash
scontrol show job 12345678
```

### Мониторинг логов в реальном времени

```bash
# Просмотр логов по мере записи
tail -f runs/flux-12345678.log

# Или последние 50 строк
tail -50 runs/flux-12345678.log

# Поиск по логам
grep "ERROR" runs/flux-12345678.log
```

### Проверка использования GPU

```bash
# На узле где работает задание
nvidia-smi

# Каждую секунду
watch -n 1 nvidia-smi
```

---

## ✅ Что ожидать в логах

### Начало работы

```
🔧 Загрузка модели из /path/to/models/flux_dev...
✅ Модель загружена!

📂 Загрузка промптов из ./prompts.json...
✅ Загружено 3 промптов
```

### Во время генерации

```
============================================================
📋 Обработка: prompt_1
============================================================

🖼️  Генерация 2 изображений БЕЗ подсказок...
🎨 Генерация 2 промптов...
   📐 Размер: 1024x1024
   🔄 Шагов дифузии: 50
   ⚡ Guidance scale: 3.5
   ✅ Сохранено: img_0000.png
   ✅ Сохранено: img_0001.png

💡 Генерация 5 изображений С подсказками...
🎨 Генерация 5 промптов...
   ✅ Сохранено: img_hint_0000.png
   ...
   ✅ Сохранено: img_hint_0004.png

✅ prompt_1 завершено!
   📝 Метаданные сохранены: results/prompt_1/metadata.json
```

### Конец работы

```
============================================================
🎉 Все готово! Результаты сохранены в: ./results
============================================================
```

---

## 🐛 Решение проблем на HPC

### Проблема: Job не запускается

```bash
# Проверить ошибку
cat runs/flux-JOBID.err

# Проверить статус
scontrol show job JOBID

# Обычные причины:
# - Неправильный путь к модели
# - GPU недоступна
# - Недостаточно ресурсов
```

### Проблема: "No such file or directory"

```bash
# Проверить пути
ls -la models/flux_dev/
ls -la prompts.json

# Убедиться что используются АБСОЛЮТНЫЕ пути в script.sbatch
pwd
```

### Проблема: "CUDA out of memory"

```bash
# В script.sbatch уменьшить:
--num_inference_steps 30   # вместо 50
# или
--height 512 --width 512   # вместо 1024
```

### Проблема: Job отменяется по таймауту

```bash
# Увеличить время в script.sbatch:
#SBATCH --time=3:00:00   # вместо 2:00:00
```

### Проблема: Модель загружается очень долго

Это нормально! Первый раз может занять 2-3 минуты.

---

## 📂 Проверка результатов

### Структура результатов

```bash
# Посмотреть структуру
tree results/

# или
find results/ -type f | head -20
```

**Ожидаемая структура:**
```
results/
├── prompt_1/
│   ├── without_hints/
│   │   ├── img_0000.png
│   │   └── img_0001.png
│   ├── with_hints/
│   │   ├── img_hint_0000.png
│   │   ├── img_hint_0001.png
│   │   ├── img_hint_0002.png
│   │   ├── img_hint_0003.png
│   │   └── img_hint_0004.png
│   └── metadata.json
```

### Проверить количество изображений

```bash
# Посчитать все .png файлы
find results/ -name "*.png" | wc -l

# По папкам
for dir in results/*/; do
    count=$(find "$dir" -name "*.png" | wc -l)
    echo "$(basename $dir): $count изображений"
done
```

### Проверить размер результатов

```bash
du -sh results/
```

---

## 💾 Копирование результатов

### На локальную машину

С локальной машины:
```bash
scp -r username@hpc:/path/to/project/SAP/flux_hpc/results ./
```

### Или по частям (если результаты большие)

```bash
# Только первый промпт
scp -r username@hpc:/path/to/project/SAP/flux_hpc/results/prompt_1 ./

# Или через rsync (безопаснее для больших объемов)
rsync -avz --progress username@hpc:/path/to/project/SAP/flux_hpc/results ./
```

---

## 🔄 Запуск нескольких заданий

### Массив заданий

```bash
# Запустить 5 заданий параллельно (по 2 одновременно)
sbatch --array=1-5%2 script.sbatch

# Проверить
squeue -u $USER
```

### Последовательные задания

```bash
# Запустить второе после первого
JOB1=$(sbatch script.sbatch | awk '{print $4}')
sbatch --dependency=afterok:$JOB1 script.sbatch
```

### Разные параметры для разных заданий

```bash
# Batch 1: Быстро (мало шагов)
sbatch --job-name=flux_fast \
       --export=NUM_STEPS=20 \
       script.sbatch

# Batch 2: Качество (много шагов)
sbatch --job-name=flux_quality \
       --export=NUM_STEPS=75 \
       script.sbatch
```

---

## ⏱️ Оценка времени

| Параметр | V100 | A100 |
|----------|------|------|
| 1 изображение (50 шагов) | 20-30 сек | 7-12 сек |
| 1024x1024 vs 512x512 | x4 медленнее | x4 медленнее |
| 1 промпт (2 без подсказок + 5 с подсказками) | ~3 мин | ~1.5 мин |
| 5 промптов | ~15 мин | ~7.5 мин |

---

## 📊 Оптимизация для HPC

### Для максимальной скорости (A100):

```bash
#SBATCH --gpus=1
#SBATCH --cpus-per-task=16
#SBATCH --time=1:00:00
#SBATCH --constraint="type_e"

python3 02_generate_images.py \
    --num_inference_steps 30  # меньше = быстрее
```

### Для лучшего качества (V100):

```bash
#SBATCH --gpus=1
#SBATCH --cpus-per-task=8
#SBATCH --time=3:00:00
#SBATCH --constraint="[type_a|type_b|type_c]"

python3 02_generate_images.py \
    --num_inference_steps 75  # больше = лучше
```

---

## 🔍 Отладка

### Запустить скрипт вручную (без SLURM)

```bash
# Для быстрого тестирования
python3 02_generate_images.py \
    --model_path ./models/flux_dev \
    --prompts_file ./prompts.json \
    --output_dir ./test_results \
    --num_inference_steps 20 \
    --height 512 --width 512
```

### Проверить модель

```bash
python3 -c "from diffusers import FluxPipeline; print(FluxPipeline.from_pretrained('./models/flux_dev'))"
```

### Проверить JSON

```bash
python3 utils.py --validate-prompts ./prompts.json
```

---

## 📞 Контакт с администратором HPC

Если нужна помощь:

```bash
# Отправить сообщение администратору
mail -s "Help needed for FLUX GPU job" admin@hpc.cluster

# Содержание:
# Job ID: (вывод из squeue)
# Error: (вывод из cat runs/flux-JOBID.err)
# Commands: (ваши команды)
```

---

## ✅ Чеклист перед запуском

- [ ] Модель скопирована: `ls models/flux_dev/` (>10GB)
- [ ] Промпты скопированы: `ls prompts.json`
- [ ] Директории созданы: `ls runs/ results/`
- [ ] Пути в script.sbatch АБСОЛЮТНЫЕ
- [ ] Окружение выбрано в script.sbatch
- [ ] Права на запуск: `ls -la script.sbatch` (должен быть x)

---

## 🎓 Полезные SLURM команды

```bash
# Статус задания
squeue -u $USER

# Отмена задания
scancel JOBID

# История
sacct -u $USER --format=JobID,JobName,State,Elapsed

# Ноды
sinfo

# Очередь в реальном времени
watch -n 2 'squeue -u $USER'

# Статус ресурсов
snode
```

---

**Готово к работе на HPC! 🚀**
