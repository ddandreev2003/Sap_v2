# 📚 FLUX HPC - Полное руководство

## 🎯 Что это?

Полностью отдельная система для генерации изображений с использованием **FLUX** модели на **HPC кластере** без интернета.

**Особенности:**
- ✅ Только генерация изображений (без ChatGPT)
- ✅ Работает на изолированном от интернета кластере
- ✅ 2 изображения БЕЗ подсказок + 5 изображений С подсказками на каждый промпт
- ✅ SLURM интеграция для HPC
- ✅ Воспроизводимые результаты (фиксированные seeds)

---

## 📖 Документация

### 🚀 Для быстрого старта (5 минут):
👉 **[QUICKSTART.md](QUICKSTART.md)** - За 5 минут до первой генерации

### 📋 Для полной инструкции:
👉 **[SETUP_GUIDE.md](SETUP_GUIDE.md)** - Полная пошаговая инструкция

### 🖥️ Для работы на HPC:
👉 **[HPC_GUIDE.md](HPC_GUIDE.md)** - Подробная инструкция для кластера

### 🔧 Технические детали:
👉 **[README.md](README.md)** - Архитектура и параметры

---

## 🗂️ Структура файлов

```
flux_hpc/
├── 📄 QUICKSTART.md              ← Начните отсюда!
├── 📄 SETUP_GUIDE.md             ← Полная инструкция
├── 📄 HPC_GUIDE.md               ← Для HPC кластера
├── 📄 README.md                  ← Технические детали
├── 📄 INDEX.md                   ← Этот файл
│
├── 🐍 01_download_models.py      ← Скрипт загрузки моделей
├── 🐍 02_generate_images.py      ← Скрипт генерации изображений
├── 🐍 utils.py                   ← Утилиты (валидация, проверка)
├── 🐍 test_generator.py          ← Локальный тест
│
├── 📝 script.sbatch              ← SLURM скрипт для генерации
├── 📝 download_models.sbatch     ← SLURM скрипт для загрузки
├── 🔧 setup_directories.sh       ← Создание директорий
├── 📊 run_batch.sh               ← Запуск batch заданий
│
├── 📋 prompts.json               ← Пример промптов
├── 📋 prompts_detailed.json      ← Расширенный пример
├── 📋 requirements.txt           ← Зависимости Python
│
├── 📂 runs/                      ← Директория для логов (создается)
├── 📂 results/                   ← Директория для результатов (создается)
└── 📂 models/                    ← Директория для моделей (создается)
```

---

## ⚡ Быстрый путь (5 минут)

1. **На локальной машине:**
   ```bash
   conda create -n flux_env python=3.10 && conda activate flux_env
   pip install -r requirements.txt
   python3 01_download_models.py --output_dir ./models
   ```

2. **На HPC кластере:**
   ```bash
   scp -r models/ username@hpc:/path/to/project/flux_hpc/
   ssh username@hpc
   cd /path/to/project/flux_hpc
   bash setup_directories.sh
   # Отредактировать script.sbatch (пути!)
   sbatch script.sbatch
   ```

3. **Смотреть результаты:**
   ```bash
   tail -f runs/flux-*.log
   scp -r username@hpc:/path/to/results ./
   ```

**→ [Полная инструкция в QUICKSTART.md](QUICKSTART.md)**

---

## 🔄 Workflow - Как это работает?

### Фаза 1️⃣: Подготовка (интернет доступ требуется)

```
[Локальная машина с интернетом]
         ↓
   01_download_models.py
         ↓
   [Скачивание FLUX модели ~25GB]
         ↓
   ./models/flux_dev/
```

### Фаза 2️⃣: Генерация (HPC без интернета)

```
[HPC кластер - скопировать модель]
         ↓
   02_generate_images.py
         ↓
   [Генерирование изображений]
         ↓
   results/
   ├── prompt_1/
   │   ├── without_hints/ (2 изображения)
   │   └── with_hints/    (5 изображений)
   └── ...
```

---

## 📊 Что генерируется?

Для каждого промпта:

```
input:  "A sunset over mountains"
        └─ hints: ["soft colors", "dramatic", "detailed", ...]

output: 7 изображений
        ├─ 2 БЕЗ подсказок   (базовый промпт)
        └─ 5 С подсказками   (промпт + разные стили)

+ metadata.json (информация о всех параметрах)
```

---

## 🚀 Основные команды

### Локальная машина (с интернетом):

```bash
# Установка
conda create -n flux_env python=3.10
conda activate flux_env
pip install -r requirements.txt

# Загрузка моделей (ОДИ раз!)
python3 01_download_models.py --output_dir ./models

# Проверка окружения
python3 utils.py --check-env

# Валидация промптов
python3 utils.py --validate-prompts prompts.json

# Создание шаблона промптов
python3 utils.py --create-template my_prompts.json

# Локальный тест (если есть GPU)
python3 test_generator.py --model_path ./models/flux_dev
```

### HPC кластер (без интернета):

```bash
# Подключение
ssh username@hpc.cluster

# Подготовка
bash setup_directories.sh

# Запуск
sbatch script.sbatch

# Мониторинг
squeue -u $USER
tail -f runs/flux-*.log

# Проверка результатов
find results/ -name "*.png" | wc -l
ls -la results/prompt_1/

# Копирование результатов
scp -r username@hpc:/path/to/results ./
```

---

## 🎯 Рекомендуемый путь обучения

### День 1: Установка и подготовка
1. Прочитайте [QUICKSTART.md](QUICKSTART.md)
2. Установите зависимости
3. Загрузите модель FLUX
4. Подготовьте промпты

### День 2: Локальное тестирование
1. Запустите локальный тест: `python3 test_generator.py`
2. Проверьте работу генератора
3. Подготовьте к отправке на HPC

### День 3: HPC запуск
1. Прочитайте [HPC_GUIDE.md](HPC_GUIDE.md)
2. Скопируйте файлы на кластер
3. Отредактируйте SLURM скрипты
4. Запустите: `sbatch script.sbatch`

### День 4: Анализ результатов
1. Собрите результаты с HPC
2. Проверьте качество изображений
3. При необходимости - повторите с другими параметрами

---

## ⚙️ Ключевые параметры

```python
# Управление генерацией в script.sbatch:

--num_without_hints    # Изображения без подсказок (по умолчанию: 2)
--num_with_hints       # Изображения с подсказками (по умолчанию: 5)
--height, --width      # Размер изображения (по умолчанию: 1024x1024)
--num_inference_steps  # Шагов дифузии (по умолчанию: 50)
--guidance_scale       # Масштаб гайданса (по умолчанию: 3.5)
--seed_base            # Начальный seed (по умолчанию: 42)
```

---

## 📈 Примерная производительность

| GPU | Время/изображение | Время на 3 промпта |
|-----|---|---|
| V100 | 15-30 сек | 5-10 мин |
| A100 | 5-10 сек | 2-3 мин |
| RTX 3090 (локально) | 30-50 сек | 10-15 мин |

---

## 🔗 Быстрые ссылки

- **Быстрый старт:** [QUICKSTART.md](QUICKSTART.md)
- **Полная инструкция:** [SETUP_GUIDE.md](SETUP_GUIDE.md)
- **HPC кластер:** [HPC_GUIDE.md](HPC_GUIDE.md)
- **Технические детали:** [README.md](README.md)

- **Скрипт генерации:** [02_generate_images.py](02_generate_images.py)
- **Утилиты:** [utils.py](utils.py)
- **Примеры промптов:** [prompts.json](prompts.json)

---

## ❓ FAQ

### Q: Нужен ли интернет для генерации на HPC?
**A:** Нет! Модель загружается один раз локально, потом скопируется на HPC.

### Q: Сколько места занимает модель?
**A:** ~25GB для FLUX.1-dev + ~2GB для зависимостей = 27GB всего

### Q: Можно ли генерировать на CPU?
**A:** Можно, но очень медленно (часы вместо минут). GPU рекомендуется.

### Q: Как изменить количество изображений на промпт?
**A:** В script.sbatch: `--num_without_hints 3 --num_with_hints 10`

### Q: Что если модель не скачалась полностью?
**A:** Запустите скрипт снова, он докачает недостающие файлы.

### Q: Где найти сгенерированные изображения?
**A:** В `results/prompt_name/without_hints/` и `results/prompt_name/with_hints/`

---

## 🆘 Решение проблем

### Проблема: "ModuleNotFoundError: No module named 'diffusers'"
```bash
pip install -r requirements.txt
```

### Проблема: "CUDA out of memory" на HPC
```bash
# В script.sbatch уменьшить:
--num_inference_steps 30  # вместо 50
--height 512 --width 512  # вместо 1024
```

### Проблема: Job не запускается на HPC
```bash
# Проверить ошибку
cat runs/flux-JOBID.err
# Убедиться что пути АБСОЛЮТНЫЕ в script.sbatch
pwd
```

**→ Полное решение проблем в [HPC_GUIDE.md](HPC_GUIDE.md#-решение-проблем-на-hpc)**

---

## 📚 Документация проектов

- [FLUX Paper](https://arxiv.org/abs/2406.16247)
- [Black Forest Labs](https://github.com/black-forest-labs/flux)
- [Diffusers](https://huggingface.co/docs/diffusers)
- [SLURM Documentation](https://slurm.schedmd.com/)

---

## 📝 Версия и обновления

- **Версия:** 1.0
- **FLUX модель:** FLUX.1-dev
- **PyTorch:** 2.7.1
- **Python:** 3.10+

---

## ✅ Следующие шаги

1. **Прямо сейчас:** Читайте [QUICKSTART.md](QUICKSTART.md) ⚡
2. **Через 5 минут:** Установите зависимости 🔧
3. **Через час:** Загрузите модель 📥
4. **Сегодня:** Первая генерация 🎨

---

**Готово к работе! Удачи! 🚀**
