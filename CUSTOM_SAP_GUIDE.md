# 🎨 ЗАПУСК FLUX С ГОТОВОЙ SAP ДЕКОМПОЗИЦИЕЙ

## Быстрый старт

У вас есть **два способа** запустить генерирование:

### 🟢 Способ 1: Командная строка (РЕКОМЕНДУЕТСЯ)

```bash
# Генерировать один пример "dragon"
python run_custom_sap.py --name dragon

# Генерировать все 7 примеров
python run_custom_sap.py --all

# С пользовательскими параметрами
python run_custom_sap.py --name woman --num-steps 80 --num-seeds 4

# Показать все доступные примеры
python run_custom_sap.py --list
```

**Это самый простой и надёжный способ!**

---

### 🟡 Способ 2: Python скрипт

```python
from run_custom_sap_quick import generate_sap_image

# Генерировать изображение
generate_sap_image("dragon", num_steps=50, num_images=1)

# Генерировать несколько изображений
generate_sap_image("shrek", num_steps=80, num_images=4)
```

---

## Команды

### Генерировать один пример

```bash
# Быстро (30 шагов, ~5-10 минут)
python run_custom_sap.py --name dragon --num-steps 30

# Оптимально (50 шагов, ~10-15 минут)
python run_custom_sap.py --name dragon --num-steps 50

# Качественно (80 шагов, ~20-25 минут)
python run_custom_sap.py --name dragon --num-steps 80
```

### Генерировать много изображений одного примера

```bash
# 4 разных изображения одного промта
python run_custom_sap.py --name shrek --num-seeds 4 --num-steps 50

# 8 изображений
python run_custom_sap.py --name woman --num-seeds 8 --num-steps 50
```

### Генерировать все примеры

```bash
# Все 7 примеров с 50 шагами
python run_custom_sap.py --all --num-steps 50

# Все примеры с 80 шагами
python run_custom_sap.py --all --num-steps 80
```

### Показать доступные примеры

```bash
python run_custom_sap.py --list
```

---

## Параметры

| Параметр | По умолчанию | Рекомендуемые значения | Описание |
|----------|---|---|---|
| `--name` | - | dragon, woman, shrek, ... | Какой пример генерировать |
| `--all` | False | - | Генерировать все примеры |
| `--list` | False | - | Показать список примеров |
| `--num-steps` | 50 | 20-100 | Количество шагов дифузии |
| `--num-seeds` | 1 | 1-8 | Количество разных изображений |
| `--seeds` | [30498] | - | Конкретные values |
| `--height` | 1024 | 512, 768, 1024 | Высота изображения |
| `--width` | 1024 | 512, 768, 1024 | Ширина изображения |
| `--device` | cuda | cuda, cpu | GPU или CPU |

---

## Примеры использования

### ⚡ Быстрый тест (5-10 минут)
```bash
python run_custom_sap.py --name pizza --num-steps 30
```

### 🎨 Обычное качество (10-15 минут)
```bash
python run_custom_sap.py --name dragon --num-steps 50
```

### ⭐ Высокое качество (20-25 минут)
```bash
python run_custom_sap.py --name woman --num-steps 80
```

### 🎬 Несколько вариантов (40-50 минут)
```bash
python run_custom_sap.py --name shrek --num-steps 50 --num-seeds 4
```

### 🏭 Все примеры (60-90 минут)
```bash
python run_custom_sap.py --all --num-steps 50
```

---

## Доступные примеры

| # | Имя | Оригинальный промт | Этапов | Переключения |
|---|---|---|---|---|
| 1 | `grown_man` | A grown man wearing a pacifier | 2 | [4] |
| 2 | `dragon` | A dragon blowing water | 2 | [3] |
| 3 | `pizza` | A pizza with grape toppings | 2 | [3] |
| 4 | `coin` | A coin floats on the surface | 2 | [4] |
| 5 | `cockatoo_parrot` | A cockatoo parrot swimming | 3 | [3, 6] |
| 6 | `woman` | A woman writing with a dart | 2 | [3] |
| 7 | `shrek` | Shrek is blue | 2 | [3] |

---

## 📂 Где найти результаты

Все сгенерированные изображения сохраняются в:

```
results_custom_sap/
├── dragon/
│   ├── dragon_seed_30498.png
│   ├── dragon_seed_40498.png
│   └── ...
├── woman/
│   ├── woman_seed_30498.png
│   └── ...
└── ...
```

---

## ❓ Частые вопросы

### Q: Какой параметр выбрать для быстрого результата?
**A:** `--num-steps 30` займет 5-10 минут на одно изображение

### Q: Как получить высокое качество?
**A:** `--num-steps 80` даст отличное качество за 20-25 минут

### Q: Сколько времени займет генерирование всех примеров?
**A:** ~80-100 минут с `--num-steps 50` и одним seed на каждый

### Q: Могу ли я генерировать на CPU?
**A:** Да: `--device cpu`, но это будет ОЧЕНЬ медленно (часы вместо минут)

### Q: Как получить разные изображения из одного промта?
**A:** Используйте `--num-seeds 4` - генерирует 4 разных изображения

### Q: Какой seed выбрать?
**A:** По умолчанию 30498 - хороший выбор. Разные seeds = разные изображения

---

## 🆘 Решение проблем

### Ошибка: "CUDA out of memory"
```bash
# Уменьшите размер изображения
python run_custom_sap.py --name dragon --height 768 --width 768

# Или уменьшите num_steps
python run_custom_sap.py --name dragon --num-steps 30
```

### Ошибка: "Module not found: SAP_pipeline_flux"
```bash
# Убедитесь что все файлы в одной директории:
# - run_custom_sap.py
# - run_custom_sap_quick.py
# - SAP_pipeline_flux.py
# - llm_interface/llm_SAP.py
```

### Ошибка: "Model not found"
```bash
# Модель FLUX скачивается автоматически (~20GB)
# Проверьте что есть ~30GB свободного места на диске
```

### Процесс очень медленный
```bash
# Убедитесь что используется GPU (cuda)
# Проверьте: nvidia-smi
# Если нет GPU - используется CPU (медленно)
```

---

## 📊 Время выполнения

| Параметры | Время | Качество |
|-----------|-------|----------|
| 30 шагов, 1 изображение | 5-10 минут | ⭐⭐ |
| 50 шагов, 1 изображение | 10-15 минут | ⭐⭐⭐ |
| 80 шагов, 1 изображение | 20-25 минут | ⭐⭐⭐⭐ |
| 50 шагов, 4 изображения | 40-60 минут | ⭐⭐⭐ (4x) |
| 7 примеров, 50 шагов | 70-100 минут | ⭐⭐⭐ |

---

## 🎯 Рекомендуемый workflow

```bash
# 1. Быстрый тест (5 минут)
python run_custom_sap.py --name dragon --num-steps 30

# 2. Если результат нравится, генерировать в качестве (20 минут)
python run_custom_sap.py --name dragon --num-steps 80

# 3. Сделать несколько вариантов (50 минут)
python run_custom_sap.py --name dragon --num-steps 50 --num-seeds 4

# 4. Генерировать все примеры (90 минут)
python run_custom_sap.py --all --num-steps 50
```

---

## 🎨 Примеры команд

```bash
# Дракон (Dragon)
python run_custom_sap.py --name dragon

# Огр (Shrek)
python run_custom_sap.py --name shrek --num-steps 80

# Женщина с дартом (Woman)
python run_custom_sap.py --name woman --num-seeds 2

# Попугай (Cockatoo Parrot) - 3 этапа
python run_custom_sap.py --name cockatoo_parrot --num-steps 60

# Мужчина с пустышкой (Grown Man)
python run_custom_sap.py --name grown_man --num-steps 50

# Все сразу!
python run_custom_sap.py --all
```

---

**Готово! Просто запустите одну из команд и наслаждайтесь генерированием 🎨**
