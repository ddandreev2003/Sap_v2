# 🚀 Быстрый старт - Combined FLUX + SAP Pipeline

## 📋 Что нужно сделать

### 1️⃣ Подготовка окружения

```bash
# Перейти в директорию проекта
cd /home/andrn/Image_Gen_Test2/SAP

# Установить зависимости (если не установлены)
pip install -r requirements.txt
```

### 2️⃣ Подготовить файл с промтами

Откройте `prompts.txt` и добавьте ваши промты (по одному на строку):

```
A beautiful sunset over mountains
A futuristic robot in a neon city
An underwater coral reef ecosystem
...
```

Или используйте готовый файл с примерами.

## 🎯 РЕКОМЕНДУЕМЫЙ WORKFLOW: Предгенерирование SAP

**Преимущества подхода с предгенерированием:**
- ⚡ **Быстрее**: LLM вызывается один раз, FLUX может вызваться много раз
- 💰 **Дешевле**: Используйте дорогой GPT для SAP один раз, дешевый FLUX много раз
- 📝 **Прозрачнее**: Проверьте/отредактируйте SAP декомпозиции перед использованием
- 💾 **Экономнее**: LLM выгружается после первого шага, освобождая память

### Шаг 1️⃣: Генерирование SAP декомпозиций (один раз)
```bash
# С GPT-4 (качественно, требует API ключ)
export OPENAI_API_KEY="your-key-here"
python workflow_example.py --step 1 --llm GPT --sap-output SAP_prompts.json

# Или с локальной Zephyr (медленно, бесплатно)
python workflow_example.py --step 1 --llm Zephyr --sap-output SAP_prompts.json
```

**Результат:** Создается файл `SAP_prompts.json` со всеми декомпозициями

### Шаг 2️⃣: Опциональная проверка
```bash
python workflow_example.py --step 1.5 --sap-output SAP_prompts.json
```

Можно отредактировать `SAP_prompts.json` если требуется улучшить декомпозиции.

### Шаг 3️⃣: Генерирование изображений с использованием сохраненных SAP (многократно)
```bash
# Используем предгенерированные SAP для экономии времени и памяти
python workflow_example.py --step 2 --mode sap --sap-output SAP_prompts.json

# Можно запустить несколько раз с разными параметрами:
python workflow_example.py --step 2 --mode sap --num-steps 50 --enable-cpu-offload
python workflow_example.py --step 2 --mode sap --num-seeds 4  # 4 изображения на промт
```

### 🏃 Полный workflow одной командой
```bash
python workflow_example.py --full --llm GPT --mode sap --num-steps 30
```

Это выполнит все шаги автоматически (генерирование SAP -> проверка -> генерирование FLUX).

---

## 🔄 АЛЬТЕРНАТИВНЫЙ WORKFLOW: Прямое использование (без предгенерирования)

Если вам не нужно сохранять SAP декомпозиции, можно использовать прямую генерацию:

### 3️⃣ Запустить генерацию

#### Вариант A: Быстрое сравнение (рекомендуется для тестирования)
```bash
python quick_launch.py --preset compare
```

#### Вариант B: Только Direct FLUX (быстро, без LLM)
```bash
python quick_launch.py --preset direct-fast
```

#### Вариант C: Только SAP (с LLM декомпозицией)
```bash
# С GPT (требует API ключ)
export OPENAI_API_KEY="your-key-here"
python quick_launch.py --preset sap-quality

# Или с локальной моделью Zephyr (бесплатно, медленнее)
python quick_launch.py --preset local-zephyr
```

#### Вариант D: Пользовательские параметры
```bash
python combined_flux_sap.py \
  --prompts-file prompts.txt \
  --mode both \
  --llm GPT \
  --seeds 30498 40123 \
  --num-inference-steps 50
```

### 4️⃣ Просмотреть результаты

Результаты будут в директории `results_combined/batch_YYYYMMDD_HHMMSS/`:
- `direct_flux/` - результаты прямой генерации
- `sap_flux/` - результаты с SAP декомпозицией

```
results_combined/
└── batch_20240130_143022/
    ├── direct_flux/
    │   ├── beautiful_sunset/
    │   │   ├── direct_seed_30498.png
    │   │   └── direct_seed_40123.png
    │   └── metadata.txt
    └── sap_flux/
        ├── beautiful_sunset/
        │   ├── sap_seed_30498.png
        │   └── sap_seed_40123.png
        └── metadata.txt
```

### 5️⃣ Сравнить результаты

```bash
# Создать отчет и HTML галерею
python compare_results.py --batch-dir results_combined/batch_20240130_143022 --all

# Или отдельно:
python compare_results.py --batch-dir results_combined/batch_20240130_143022 --report report.txt
python compare_results.py --batch-dir results_combined/batch_20240130_143022 --gallery gallery/
```

Откройте `gallery/comparison.html` в браузере для интерактивного сравнения.

---

## 🎯 Рекомендуемые сценарии использования

### Для быстрой проверки концепции (5-10 минут)
```bash
python quick_launch.py --preset compare
```

### Для получения высококачественных результатов (15-30 минут)
```bash
export OPENAI_API_KEY="your-key"
python quick_launch.py --preset sap-quality
```

### Для локального тестирования без API (бесплатно, 10-20 минут)
```bash
python quick_launch.py --preset local-zephyr
```

### Для полного анализа и сравнения (30-60 минут)
```bash
export OPENAI_API_KEY="your-key"
python quick_launch.py --preset full-compare
python compare_results.py --batch-dir results_combined/batch_*/batch_* --all
```

---

## 🔑 Установка API ключа (для GPT режима)

### Linux/Mac
```bash
export OPENAI_API_KEY="sk-..."
# Проверить:
echo $OPENAI_API_KEY
```

### Windows (PowerShell)
```powershell
$env:OPENAI_API_KEY = "sk-..."
# Проверить:
$env:OPENAI_API_KEY
```

### Постоянно (Linux/Mac)
Добавьте в `~/.bashrc` или `~/.zshrc`:
```bash
export OPENAI_API_KEY="sk-..."
```

Затем выполните:
```bash
source ~/.bashrc  # или source ~/.zshrc
```

---

## 📊 Структура результатов

```
results_combined/
└── batch_20240130_143022/           # Временная метка генерации
    ├── direct_flux/                 # Результаты Direct FLUX
    │   ├── prompt_name_1/
    │   │   ├── direct_seed_30498.png
    │   │   ├── direct_seed_40123.png
    │   │   └── ...
    │   ├── prompt_name_2/
    │   │   └── ...
    │   └── metadata.txt             # Параметры генерации
    ├── sap_flux/                    # Результаты SAP FLUX
    │   ├── prompt_name_1/
    │   │   ├── sap_seed_30498.png
    │   │   ├── sap_seed_40123.png
    │   │   └── ...
    │   ├── prompt_name_2/
    │   │   └── ...
    │   └── metadata.txt             # Параметры + LLM информация
    └── gallery/                     # HTML галерея для сравнения
        ├── comparison.html
        ├── direct_flux/             # Символические ссылки
        └── sap_flux/
```

---

## ⚙️ Основные параметры

| Параметр | Default | Описание |
|----------|---------|---------|
| `--height` | 1024 | Высота изображения |
| `--width` | 1024 | Ширина изображения |
| `--num-inference-steps` | 50 | Количество шагов дифузии (чем больше, тем качественнее) |
| `--guidance-scale` | 3.5 | Масштаб гайданса (как строго следовать промту) |
| `--seeds` | 30498 | Seeds для воспроизводимости |
| `--llm` | GPT | LLM для SAP (GPT или Zephyr) |
| `--mode` | both | Режим: direct, sap, both |

---

## 📈 Сравнение методов

| Аспект | Direct FLUX | SAP FLUX |
|--------|------------|---------|
| **Скорость** | ⚡⚡⚡ Быстро | ⚡⚡ Медленнее |
| **Качество** | ⭐⭐ Хорошее | ⭐⭐⭐ Лучше |
| **Детализация** | Базовая | Высокая |
| **Требует LLM** | ❌ Нет | ✅ Да |
| **Цена** | 💰 Низко | 💰💰 Выше |
| **Лучше для** | Быстрого прототипирования | Финальных результатов |

---

## 🆘 Решение проблем

### ❌ "CUDA out of memory"
```bash
# Используйте меньше seeds или меньше разрешение:
python combined_flux_sap.py \
  --seeds 30498 \
  --height 768 \
  --width 768 \
  --num-inference-steps 30
```

### ❌ "OpenAI API error"
```bash
# Проверьте API ключ:
echo $OPENAI_API_KEY

# Или используйте Zephyr (бесплатно):
python quick_launch.py --preset local-zephyr
```

### ❌ "Model not found"
```bash
# Убедитесь, что у вас есть интернет и доступ к HuggingFace
# Проверьте HuggingFace токен (если приватный доступ):
huggingface-cli login
```

### ⚠️ "Модель загружается очень долго (первый запуск)"
Это нормально! Первый запуск загружает модель (10-30 GB).
Последующие запуски будут быстрее.

---

## 📚 Полная документация

Для более подробной информации смотрите:
- [COMBINED_FLUX_SAP_README.md](COMBINED_FLUX_SAP_README.md) - Полная документация
- [README.md](README.md) - Информация о проекте
- [flux_hpc/README.md](flux_hpc/README.md) - Документация FLUX HPC

---

## 🎬 Примеры результатов

После запуска вы получите:

### Direct FLUX Results
![Direct Example] 
- Быстрая генерация
- Базовое качество
- Прямое применение промта

### SAP FLUX Results
![SAP Example]
- Детализированные изображения
- Лучшее качество
- Результат декомпозиции через LLM

---

**Приятной работы! 🎨**

Если у вас есть вопросы или возникли проблемы, смотрите [COMBINED_FLUX_SAP_README.md](COMBINED_FLUX_SAP_README.md)
